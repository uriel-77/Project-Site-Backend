# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Usuario autenticable del sistema"""
type Alumno {
  apellido: String!
  contenidosCompletados: [Contenido!]
  email: String!
  estado: EstadoUsuario!
  grupo: String!
  id: Int!
  nombre: String!
  rol: RolUsuario!
}

type Asignacion {
  activa: Boolean!
  contenidoId: Int
  descripcion: String!
  entregable: Boolean!
  grupo: String!
  id: ID!
  orden: Int!
  periodo: Int!
  porcentaje: Int!
  rubrica: String!
  titulo: String!
  videos: [Video!]
}

type AuthPayload {
  token: String!
  usuario: Alumno!
}

type CalificacionAsignacion {
  alumnoId: Int!
  asignacionId: Int!
  calificacion: Int!
  fechaCalificacion: String!
  grupo: String!
  id: ID!
  observaciones: String
  parcial: Int!
}

type Contenido {
  asignaciones: [Asignacion!]
  contenido: [String!]!
  descripcion: String!
  id: ID!
  orden: Int!
  tipo: TipoContenido!
  tipoMateria: TipoMateria!
  titulo: String!
  unidad: Unidad
  unidadId: Int!
  url_recurso: String
  videos: [Video!]
}

input CreateAlumnoInput {
  apellido: String!
  email: String!
  grupo: String!
  nombre: String!
  password: String!
}

input CreateAsignacionInput {
  activa: Boolean! = true
  contenidoId: Int
  descripcion: String! = ""
  entregable: Boolean! = true
  grupo: String! = ""
  orden: Int! = 0
  periodo: Int!
  porcentaje: Int! = 0
  rubrica: String! = ""
  titulo: String!
  videoIds: [Int!]! = []
}

input CreateContenidoInput {
  contenido: [String!]!
  descripcion: String!
  orden: Int!
  tipo: TipoContenido!
  tipoMateria: TipoMateria!
  titulo: String!
  unidadId: Int!
  url_recurso: String
}

input CreateUnidadInput {
  nombre: String!
}

input CreateUsuarioInput {
  apellido: String!
  email: String!
  estado: EstadoUsuario! = ACTIVO
  grupo: String! = ""
  nombre: String!
  password: String!
  rol: RolUsuario!
}

input CreateVideoInput {
  asignacionIds: [Int!]! = []
  contenidoId: Int
  descripcion: String! = ""
  publicado: Boolean! = true
  tipos: [TipoVideo!]!
  titulo: String!
  youtubeUrl: String!
}

type Entrega {
  alumnoId: Int!
  archivoBase64: String!
  asignacionId: Int!
  estado: String!
  fechaEntrega: String!
  grupo: String!
  id: ID!
  mimeType: String!
  nombreArchivo: String!
  parcial: Int!
  tamano: Int!
}

enum EstadoUsuario {
  ACTIVO
  INACTIVO
}

input GuardarCalificacionAsignacionInput {
  alumnoId: Int!
  asignacionId: Int!
  calificacion: Int!
  grupo: String!
  observaciones: String
  parcial: Int!
}

type Insignia {
  icon: String!
  id: Int!
  nivel: String!
  nombre: String!
}

input LoginAlumnoInput {
  email: String!
  password: String!
}

type Mutation {
  actualizarAsignacion(datos: UpdateAsignacionInput!): Asignacion!
  actualizarContenido(datos: UpdateContenidoInput!): Contenido!
  actualizarMiPerfil(datos: UpdateUsuarioInput!): Alumno!
  actualizarUnidad(updateUnidadInput: UpdateUnidadInput!): Unidad!
  actualizarVideo(datos: UpdateVideoInput!): Video!
  completarContenido(alumnoId: Int!, contenidoId: Int!): Alumno
  crearAlumno(datos: CreateAlumnoInput!): Alumno!
  crearAsignacion(datos: CreateAsignacionInput!): Asignacion!
  crearContenido(datos: CreateContenidoInput!): Contenido!
  crearUnidad(createUnidadInput: CreateUnidadInput!): Unidad!
  crearUsuario(datos: CreateUsuarioInput!): Alumno!
  crearVideo(datos: CreateVideoInput!): Video!
  eliminarAsignacion(id: Int!): Asignacion!
  eliminarContenido(id: Int!): Contenido!
  eliminarUnidad(id: Int!): Unidad!
  eliminarUsuario(id: Int!): Alumno!
  eliminarVideo(id: Int!): Video!
  guardarCalificacionAsignacion(datos: GuardarCalificacionAsignacionInput!): CalificacionAsignacion!
  iniciarSesion(datos: LoginAlumnoInput!): AuthPayload!
  registrarEntrega(datos: RegistrarEntregaInput!): Entrega!

  """Valida el código y actualiza la contraseña"""
  restablecerPassword(datos: RestablecerPasswordInput!): Boolean!

  """Genera un código y lo envía por correo"""
  solicitarRecuperacionPassword(datos: SolicitarRecuperacionInput!): Boolean!
}

type Query {
  asignacion(id: Int!): Asignacion
  asignaciones(contenidoId: Int, grupo: String, periodo: Int): [Asignacion!]!
  calificacionesAsignacion(alumnoId: Int, asignacionId: Int, grupo: String, parcial: Int): [CalificacionAsignacion!]!
  contenido(id: Int!): Contenido
  contenidos(tipoMateria: TipoMateria): [Contenido!]!
  entregas(alumnoId: Int, asignacionId: Int, grupo: String, parcial: Int): [Entrega!]!
  grupos: [String!]!
  insignias: [Insignia!]!
  ping: String!
  rubricasPorPeriodo(periodo: Int!): [Rubrica!]!
  unidad(id: Int!): Unidad
  unidades(tipoMateria: TipoMateria): [Unidad!]!
  usuarios: [Alumno!]!
  video(id: Int!): Video
  videos(asignacionId: Int, contenidoId: Int, publicado: Boolean): [Video!]!
}

input RegistrarEntregaInput {
  alumnoId: Int!
  archivoBase64: String!
  asignacionId: Int!
  grupo: String!
  mimeType: String!
  nombreArchivo: String!
  parcial: Int!
  tamano: Int!
}

input RestablecerPasswordInput {
  codigo: String!
  email: String!
  nuevoPassword: String!
}

enum RolUsuario {
  ADMINISTRADOR
  ALUMNO
  MODERADOR
}

type Rubrica {
  criterios: String!
  id: Int!
  periodo: Int!
  titulo: String!
  url: String!
}

input SolicitarRecuperacionInput {
  email: String!
}

enum TipoContenido {
  LECCION
  RECURSO
  TAREA
}

enum TipoMateria {
  COMPILADORES
  TEORIA_DE_LA_COMPUTACION
  TEORIA_DE_LENGUAJES
}

enum TipoVideo {
  ACTIVIDAD
  APRENDIZAJE
  OTRO
}

type Unidad {
  contenidos: [Contenido!]
  id: Int!
  nombre: String!
}

input UpdateAsignacionInput {
  activa: Boolean
  contenidoId: Int
  descripcion: String
  entregable: Boolean
  grupo: String
  id: Int!
  orden: Int
  periodo: Int
  porcentaje: Int
  rubrica: String
  titulo: String
  videoIds: [Int!]
}

input UpdateContenidoInput {
  contenido: [String!]
  descripcion: String
  id: Int!
  orden: Int
  tipo: TipoContenido
  tipoMateria: TipoMateria
  titulo: String
  unidadId: Int
  url_recurso: String
}

input UpdateUnidadInput {
  id: Int!
  nombre: String
}

input UpdateUsuarioInput {
  apellido: String
  email: String
  estado: EstadoUsuario
  grupo: String
  id: Int!
  nombre: String
  password: String
  rol: RolUsuario
}

input UpdateVideoInput {
  asignacionIds: [Int!]
  contenidoId: Int
  descripcion: String
  id: Int!
  publicado: Boolean
  tipos: [TipoVideo!]
  titulo: String
  youtubeUrl: String
}

type Video {
  asignaciones: [Asignacion!]
  contenidoId: Int
  descripcion: String!
  id: ID!
  publicado: Boolean!
  tipos: [TipoVideo!]!
  titulo: String!
  youtubeId: String!
  youtubeUrl: String!
}