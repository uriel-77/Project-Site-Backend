# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Entidad que representa a un alumno del sistema"""
type Alumno {
  contenidosCompletados: [Contenido!]
  email: String!
  id: Int!
  nombre: String!
}

type Contenido {
  contenido: [String!]!
  descripcion: String!
  id: ID!
  orden: Int!
  tipo: TipoContenido
  tipoMateria: TipoMateria!
  titulo: String!
  unidad: Unidad
  unidadId: Int!
  url_recurso: String
}

input CreateAlumnoInput {
  email: String!
  nombre: String!
}

input CreateUnidadInput {
  nombre: String!
}

type Insignia {
  icon: String!
  id: Int!
  nivel: String!
  nombre: String!
}

type Mutation {
  completarContenido(alumnoId: Int!, contenidoId: Int!): Alumno
  crearAlumno(datos: CreateAlumnoInput!): Alumno!
  createUnidad(createUnidadInput: CreateUnidadInput!): Unidad!
  removeUnidad(id: Int!): Unidad!
  updateUnidad(updateUnidadInput: UpdateUnidadInput!): Unidad!
}

type Query {
  alumnos: [Alumno!]!
  contenido(id: Int!): Contenido
  contenidos: [Contenido!]!
  insignias: [Insignia!]!
  rubricasPorPeriodo(periodo: Int!): [Rubrica!]!
  unidad(id: Int!): Unidad
  unidades: [Unidad!]!
}

type Rubrica {
  criterios: String!
  id: Int!
  periodo: Int!
  titulo: String!
  url: String!
}

enum TipoContenido {
  LECCION
  RECURSO
  TAREA
}

enum TipoMateria {
  COMPILADORES
  TEORIA_DE_LA_COMPUTACION
  TEORIA_DE_LENGUAJES
}

type Unidad {
  contenidos: [Contenido!]
  id: Int!
  nombre: String!
}

input UpdateUnidadInput {
  id: Int!
  nombre: String
}